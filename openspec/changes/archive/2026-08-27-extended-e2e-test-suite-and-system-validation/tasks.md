## 1. Command Palette & Search Test Suite

- [x] 1.1 Create `__tests__/api/command-palette-and-search.test.ts` and verify search filtering, accent tolerance, and state lookup.

## 2. Routing & Redirects Test Suite

- [x] 2.1 Create `__tests__/api/routing-and-redirects.test.ts` and test all redirects in `next.config.ts`.

## 3. Theme & WCAG Contrast Audit Test Suite

- [x] 3.1 Create `__tests__/api/theme-and-contrast.test.ts` and verify luminance ratios for Sunlight and Dark palettes.

## 4. Native GIS Engine Lifecycle Test Suite

- [x] 4.1 Create `__tests__/api/native-gis-lifecycle.test.ts` and test layer styling for all 7 thematic layers.

## 5. Full Validation & Documentation Update

- [x] 5.1 Run all tests (`npm test` and `cd backend && py -m pytest tests`), TypeScript check (`npx tsc --noEmit`), and Turbopack build (`npm run build`). Update `AGENTS.md` and `README.md`.
